# Pour utiliser uv :

1. Installez uv :
    ```bash
    pip install uv
    ```

2. Une fois dans le répertoire, lancez :
    ```bash
    uv sync
    ```

3. Utilisez le `.venv` comme interpréteur dans Jupyter.